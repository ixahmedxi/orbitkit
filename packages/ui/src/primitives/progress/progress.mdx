import { Canvas, Meta, Source } from '@storybook/blocks';

import { RadixPrimitiveDocsButton } from '../../storybook-utils/primitive-docs-button';
import * as ProgressStories from './progress.stories';

<Meta of={ProgressStories} />

<RadixPrimitiveDocsButton name="progress" />

# Progress

Displays an indicator showing the completion progress of a task, typically displayed as a progress bar.

## Preview

<Canvas of={ProgressStories.Default} />

## Usage

export const importCode = `import { Progress } from "@orbitkit/ui/progress";`;

export const usageCode = `<Progress value={33} />`;

<Source code={importCode} language="ts" dark />
<Source code={usageCode} language="tsx" dark />
